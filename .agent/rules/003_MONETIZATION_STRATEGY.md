# 營利策略

刻意將 `src/features/free/` 和 `src/features/paid/` 分開。這種結構允許：
1.  **程式碼組織清晰**：開發者可以輕鬆區分免費和進階功能。
2.  **靈活建置**：我們可以設定建置腳本（例如使用 Webpack 或 Rollup）來建立不同的擴充功能套件：
    - **免費版**：僅包含 `free/` 目錄中的模組。
    - **進階版**：包含 `free/` 和 `paid/` 目錄中的模組。
